server:
  port: 8189
  servlet:
    context-path: /marketplace
spring:
  session:
    jdbc:
      initialize-schema: always
    timeout:
      second: 900
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQL95Dialect
      show_sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/product_db?currentSchema=product_sh
    username: postgres
    password: 1qaz@WSX
  flyway:
    enabled: true
    location: classpath:/db/migration/market/msproduct



